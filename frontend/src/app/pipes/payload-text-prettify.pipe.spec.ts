import { PayloadTextPrettifyPipe } from './payload-text-prettify.pipe';

describe('PayloadTextPrettifyPipe', () => {
  it('create an instance', () => {
    const pipe = new PayloadTextPrettifyPipe();
    expect(pipe).toBeTruthy();
  });
});
