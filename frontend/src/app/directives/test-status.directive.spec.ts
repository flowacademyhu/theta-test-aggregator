import { TestStatusDirective } from './test-status.directive';

describe('TestStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new TestStatusDirective();
    expect(directive).toBeTruthy();
  });
});
